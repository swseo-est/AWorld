# 프로젝트 AWorld: 핵심 기능 및 아키텍처

이 문서는 AWorld 프로젝트의 고수준 개요, 아키텍처, 그리고 주요 구성 요소 간의 관계를 제공합니다. 이 문서의 목표는 리버스 엔지니어링 및 유사 프로젝트 생성을 용이하게 하는 것입니다.

## 1. 프로젝트 개요

AWorld는 자율 AI 에이전트를 구축, 배포, 훈련하기 위한 정교한 차세대 프레임워크입니다. 핵심 철학은 **에이전트의 자기 개선**에 중점을 두어, 에이전트가 자신의 경험으로부터 배우고 진화할 수 있도록 합니다.

이 프레임워크는 다음을 지원하도록 설계되었습니다:
-   **단일 에이전트 구축**: 특정 기술, 도구, 메모리를 가진 개별 에이전트 생성.
-   **다중 에이전트 시스템 (MAS)**: 여러 에이전트("Swarm"이라 칭함) 간의 복잡한 워크플로우 및 협업 토폴로지 조정.
-   **에이전트 훈련**: 에이전트의 운영에서 수집된 데이터를 기반으로 에이전트를 훈련하고 미세 조정하는 인프라 제공.
-   **클라우드 네이티브 배포**: 확장 가능하고 분산된 컨테이너 환경에서 에이전트 실행.

## 2. 핵심 의존성

이 프로젝트는 최신 파이썬 스택에 의존합니다. 주요 의존성은 프로젝트의 아키텍처를 잘 보여줍니다:

-   **LLM 연동**: `openai`, `tiktoken`은 대규모 언어 모델(LLM)과 상호 작용하는 데 사용됩니다.
-   **데이터 및 설정**: `pydantic`, `pyyaml`은 강력한 데이터 유효성 검사 및 설정 관리에 사용됩니다.
-   **웹 및 API 제공**: `fastapi`, `uvicorn`, `gunicorn`은 에이전트와 서비스를 REST API로 노출하는 기반을 제공합니다.
-   **분산 시스템**: `grpcio`는 서비스 간 고성능 통신에 사용되며, `kubernetes`는 컨테이너 오케스트레이션에 사용됩니다.
-   **관찰 가능성**: `opentelemetry`는 분산 추적 및 메트릭을 위해 통합되어, 복잡한 에이전트 상호 작용을 디버깅하고 모니터링하는 데 필수적입니다.
-   **데이터베이스**: `sqlalchemy`, `psycopg2-binary`는 분산 배포 환경에서 에이전트 메모리, 상태 및 기타 중요 데이터를 영구 저장하기 위해 PostgreSQL 데이터베이스를 사용함을 나타냅니다.
-   **도구**: `selenium`, `playwright`(추정), `e2b_code_interpreter`는 에이전트가 웹 브라우저와 상호 작용하고 코드를 실행하는 강력한 기능을 제공합니다.

## 3. 고수준 아키텍처 및 폴더 구조

프로젝트는 논리적으로 여러 핵심 구성 요소로 나뉘며, 각 구성 요소는 최상위 디렉토리에 위치합니다.

| 디렉토리                | 목적                                                                                                                                                                                                            | 주요 기술/개념                                      |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| **`aworld/`**           | **핵심 프레임워크 라이브러리**. 에이전트, 스웜, 태스크, 도구, 메모리 생성을 위한 모든 기본 빌딩 블록을 포함합니다. 개발자가 에이전트 애플리케이션을 구축할 때 사용하는 기본 패키지입니다.                          | Python, `pydantic`, `openai`, `fastapi`                 |
| **`aworlddistributed/`**| **분산 배포 패키지**. AWorld를 확장 가능한 클라우드 네이티브 애플리케이션으로 실행하기 위한 인프라를 제공합니다. API 서버, 작업 관리, 에이전트 도구를 위한 마이크로서비스를 포함합니다.                 | `gunicorn`, `grpcio`, `sqlalchemy`, `docker-compose.yaml` |
| **`env/`**              | **실행 환경**. 에이전트가 작동할 특정 컨테이너화된 환경을 정의합니다. 표준화된 평가(예: GAIA)를 위해 사전 구성된 도구(MCP 서버) 세트를 갖춘 복잡한 설정인 경우가 많습니다.                                   | Docker, Shell 스크립트                                  |
| **`examples/`**         | **사용 예제**. 기본 에이전트 생성부터 복잡한 다중 에이전트 워크플로우에 이르기까지 프레임워크의 기능을 사용하는 방법을 보여주는 포괄적인 코드 샘플 모음입니다. 프레임워크 학습을 위한 핵심 자료입니다.         | Python 스크립트                                         |
| **`train/`**            | **에이전트 훈련 및 자기 개선**. 수집된 경험을 바탕으로 에이전트를 훈련시키기 위한 코드, 어댑터, 예제를 포함합니다. 외부 훈련 프레임워크와 통합됩니다.                                                            | Python, ML 프레임워크 (Swift, Verl)                     |
| **`docs/`**             | **프로젝트 문서**. 사용자 대상 문서 웹사이트의 소스 파일입니다.                                                                                                                                                 | Markdown                                                |
| **`tests/`**            | **자동화 테스트**. 프레임워크 구성 요소의 정확성을 보장하는 단위 및 통합 테스트입니다.                                                                                                                         | `pytest`(추정)                                          |

## 4. 관계 다이어그램

```
+---------------------+      +------------------------+      +---------------------+
|      `examples/`    |----->|        `aworld/`       |<-----|       `tests/`      |
| (프레임워크 사용법) |      |    (핵심 프레임워크)   |      | (프레임워크 검증)   |
+---------------------+      +-----------+------------+      +---------------------+
                                         |
                                         | 확장
                                         |
                         +---------------+----------------+
                         |                                |
+------------------------+      +------------------------+      +------------------------+
| `aworlddistributed/`   |<-----|         `env/`         |----->|        `train/`        |
| (대규모 프레임워크 실행) |      | (에이전트 환경 정의)   |      | (프레임워크 내 에이전트 개선) |
+------------------------+      +------------------------+      +------------------------+

```

### 컴포넌트 상호작용 흐름:

1.  개발자는 **`aworld/`**의 컴포넌트를 사용하여 에이전트와 그 로직을 정의합니다.
2.  **`examples/`**를 참조하여 코드 구조를 이해할 수 있습니다.
3.  확장 가능한 배포를 위해, 강력한 서버 환경에서 핵심 프레임워크를 실행하는 **`aworlddistributed/`**가 제공하는 인프라를 사용합니다.
4.  에이전트는 작업을 수행하는 데 필요한 도구를 제공하는 특정 **`env/`** 설정 내에서 실행됩니다.
5.  에이전트가 수집한 경험과 데이터는 **`train/`** 파이프라인에 입력되어 시간이 지남에 따라 에이전트를 미세 조정하고 개선하는 데 사용됩니다.
6.  **`tests/`**는 `aworld/`의 핵심 컴포넌트가 예상대로 작동하는지 확인합니다.
7.  **`docs/`**는 프로젝트에 대한 사람이 읽을 수 있는 설명을 제공합니다.
